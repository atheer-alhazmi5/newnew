@{
    ViewBag.Title = "التصنيفات التنظيمية";
    ViewBag.PageName = "التصنيفات التنظيمية";
}

<style>
.cls-modal { border-radius:20px; border:none; overflow:hidden; box-shadow:0 20px 60px rgba(0,0,0,.15); }
.cls-modal-header { background:var(--sa-800); padding:24px 28px; position:relative; }
.cls-modal-header .btn-close-x { position:absolute; top:16px; left:16px; background:none; border:none; color:rgba(255,255,255,.7); font-size:22px; cursor:pointer; padding:4px; line-height:1; transition:color .2s; }
.cls-modal-header .btn-close-x:hover { color:#fff; }
.cls-modal-title { color:#fff; font-weight:800; font-size:20px; margin:0; }
.cls-modal-subtitle { color:rgba(255,255,255,.7); font-size:14px; margin-top:4px; }
.cls-modal-body { padding:28px; }
.cls-modal-footer { padding:20px 28px; display:flex; gap:12px; border:none; justify-content:flex-start; }
.cls-save-btn { padding:12px 32px; border-radius:12px; font-size:15px; font-weight:700; font-family:'Cairo',sans-serif; border:none; cursor:pointer; background:var(--sa-600); color:#fff; transition:all .2s; display:inline-flex; align-items:center; gap:8px; }
.cls-save-btn:hover { background:var(--sa-700); transform:translateY(-1px); }
.cls-cancel-btn { padding:12px 32px; border-radius:12px; font-size:15px; font-weight:700; font-family:'Cairo',sans-serif; border:none; cursor:pointer; background:#6b7280; color:#fff; transition:all .2s; }
.cls-cancel-btn:hover { background:#4b5563; }
.cls-section-title { font-weight:700; font-size:15px; color:var(--sa-800); margin-bottom:20px; display:flex; align-items:center; gap:8px; padding-bottom:12px; border-bottom:2px solid var(--sa-100); }
.cls-section-title i { font-size:18px; }
.required-star { color:#ef4444; margin-right:4px; font-weight:700; }
.cls-action-btn { min-width:80px; border-radius:10px; font-weight:700; padding:7px 14px; font-size:12px; line-height:1.3; display:inline-flex; align-items:center; gap:5px; justify-content:center; cursor:pointer; transition:all .15s; border:2px solid transparent; font-family:'Cairo',sans-serif; white-space:nowrap; }
.cls-action-btn-detail { border-color:var(--info-500); color:var(--info-500); background:#fff; }
.cls-action-btn-detail:hover { background:var(--info-50); }
.cls-action-btn-edit { border-color:var(--sa-600); color:var(--sa-600); background:#fff; }
.cls-action-btn-edit:hover { background:var(--sa-50); }
.cls-action-btn-delete { border-color:var(--error-500); color:var(--error-500); background:#fff; }
.cls-action-btn-delete:hover { background:var(--error-50); }
.cls-status-pill { display:inline-flex; align-items:center; gap:6px; padding:6px 14px; border-radius:999px; font-weight:700; font-size:12px; }
.cls-status-pill.active { background:#dcf5e8; color:#0f9f5c; }
.cls-status-pill.inactive { background:#fde8e8; color:#ef4444; }
.cls-status-dot { width:8px; height:8px; border-radius:999px; display:inline-block; }
.cls-status-pill.active .cls-status-dot { background:#0f9f5c; }
.cls-status-pill.inactive .cls-status-dot { background:#ef4444; }
.cls-color-circle { width:28px; height:28px; border-radius:50%; display:inline-block; border:2px solid rgba(0,0,0,.1); vertical-align:middle; box-shadow:0 1px 4px rgba(0,0,0,.08); }
.table > :not(caption) > * > * { padding-top:0.75rem; padding-bottom:0.75rem; vertical-align:middle; }
.table thead th { background:var(--gray-50) !important; color:var(--gray-700); font-size:13px; font-weight:700; text-align:center; white-space:nowrap; border-bottom:2px solid var(--gray-200); }
.table tbody tr { transition:background .15s; }
.table tbody tr:hover { background:var(--sa-25); }
.table tbody td { border-bottom:1px solid var(--gray-100); }
.cls-color-picker-wrap { position:relative; display:inline-block; }
.cls-color-picker-wrap input[type="color"] { width:52px; height:52px; border:2px solid var(--gray-200); border-radius:14px; cursor:pointer; padding:3px; background:#fff; transition:border-color .2s; }
.cls-color-picker-wrap input[type="color"]:hover { border-color:var(--sa-400); }
.cls-color-picker-wrap input[type="color"]::-webkit-color-swatch-wrapper { padding:2px; }
.cls-color-picker-wrap input[type="color"]::-webkit-color-swatch { border-radius:10px; border:none; }
.cls-color-hex { font-size:13px; color:var(--gray-500); font-weight:600; direction:ltr; text-align:center; font-family:'Courier New',monospace; background:var(--gray-50); padding:4px 10px; border-radius:8px; border:1px solid var(--gray-200); }
.cls-detail-row { display:flex; align-items:flex-start; gap:12px; padding:14px 0; border-bottom:1px solid var(--gray-100); }
.cls-detail-row:last-child { border-bottom:none; }
.cls-detail-label { font-weight:700; font-size:14px; color:var(--gray-600); min-width:120px; }
.cls-detail-value { font-size:14px; color:var(--gray-900); flex:1; }
.cls-search-box { position:relative; max-width:320px; }
.cls-search-box input { padding-right:42px; border-radius:12px; border:2px solid var(--gray-200); font-size:14px; height:44px; }
.cls-search-box input:focus { border-color:var(--sa-400); box-shadow:0 0 0 3px rgba(37,147,95,.12); }
.cls-search-box .cls-search-icon { position:absolute; right:14px; top:50%; transform:translateY(-50%); color:var(--gray-400); font-size:16px; pointer-events:none; }
.cls-modal-body .form-control,
.cls-modal-body .form-select { border-radius:10px; border:2px solid var(--gray-200); padding:10px 14px; transition:border-color .2s, box-shadow .2s; }
.cls-modal-body .form-control:focus { border-color:var(--sa-400); box-shadow:0 0 0 3px rgba(37,147,95,.1); }
.cls-modal-body .form-label { font-weight:700; font-size:14px; color:var(--gray-700); margin-bottom:8px; }
.cls-modal-body .form-check-input:checked { background-color:var(--sa-600); border-color:var(--sa-600); }
.cls-page-header { display:flex; justify-content:space-between; align-items:center; margin-bottom:28px; flex-wrap:wrap; gap:12px; }
.cls-page-header .cls-add-btn { border-radius:12px; padding:10px 28px; font-weight:700; font-size:14px; display:inline-flex; align-items:center; gap:8px; }
</style>

<div class="cls-page-header">
    <div>
        <h2 class="page-title" style="margin:0;">التصنيفات التنظيمية</h2>
        <p style="color:var(--gray-500);font-size:14px;margin:6px 0 0;">تصنيفات الوحدات في الهيكل التنظيمي</p>
    </div>
    <button class="btn btn-primary cls-add-btn" onclick="clsShowAddModal()">
        <i class="bi bi-plus-lg"></i>إضافة تصنيف
    </button>
</div>

<!-- Search Bar -->
<div class="d-flex gap-3 mb-4 flex-wrap align-items-center">
    <div class="cls-search-box flex-grow-1" style="max-width:400px;">
        <i class="bi bi-search cls-search-icon"></i>
        <input type="text" class="form-control" id="clsSearchInput" placeholder="البحث عن تصنيف بالاسم..." oninput="clsFilter()">
    </div>
</div>

<!-- Table -->
<div class="card">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table mb-0">
                <thead>
                    <tr>
                        <th style="width:70px;">الترتيب</th>
                        <th>التصنيف</th>
                        <th style="width:130px;">اللون</th>
                        <th>الوصف</th>
                        <th style="width:110px;">التفعيل</th>
                        <th style="width:260px;">الإجراءات</th>
                    </tr>
                </thead>
                <tbody id="clsBody">
                    <tr><td colspan="6" class="text-center py-4"><div class="spinner-border" style="color:var(--sa-600);"></div></td></tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div id="clsPaginationContainer" class="d-flex justify-content-center mt-4"></div>

<!-- Add Modal -->
<div class="modal fade" id="clsAddModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content cls-modal">
            <div class="cls-modal-header">
                <button type="button" class="btn-close-x" data-bs-dismiss="modal"><i class="bi bi-x-lg"></i></button>
                <h5 class="cls-modal-title"><i class="bi bi-plus-circle" style="margin-left:8px;"></i>إضافة تصنيف جديد</h5>
                <div class="cls-modal-subtitle">أدخل بيانات التصنيف الجديد</div>
            </div>
            <div class="cls-modal-body">
                <div class="cls-section-title"><i class="bi bi-info-circle-fill"></i>معلومات التصنيف</div>

                <div class="mb-3">
                    <label class="form-label">اسم التصنيف<span class="required-star">*</span></label>
                    <input type="text" class="form-control" id="clsAddName" >
                </div>
                <div class="mb-3">
                    <label class="form-label">الوصف</label>
                    <textarea class="form-control" id="clsAddDescription" rows="3" placeholder="وصف مختصر لهذا التصنيف"></textarea>
                </div>
                <div class="row mb-3">
                    <div class="col-6">
                        <label class="form-label">اللون</label>
                        <div class="d-flex align-items-center gap-3">
                            <div class="cls-color-picker-wrap">
                                <input type="color" id="clsAddColor" value="#25935F" oninput="document.getElementById('clsAddColorHex').textContent=this.value">
                            </div>
                            <div class="cls-color-hex" id="clsAddColorHex" style="margin-top:0;">#25935F</div>
                        </div>
                    </div>
                    <div class="col-6">
                        <label class="form-label">التفعيل</label>
                        <div class="d-flex align-items-center gap-3 mt-2">
                            <div class="form-check form-switch m-0">
                                <input class="form-check-input" type="checkbox" id="clsAddIsActive" checked style="width:48px;height:24px;">
                            </div>
                            <label class="form-check-label" for="clsAddIsActive" id="clsAddIsActiveLabel" style="font-weight:600;">مفعل</label>
                        </div>
                    </div>
                </div>

                <div id="clsAddError" class="alert alert-danger d-none" style="font-size:14px;"></div>
            </div>
            <div class="cls-modal-footer">
                <button class="cls-save-btn" onclick="clsSubmitAdd()">
                    <i class="bi bi-plus-lg"></i>إضافة التصنيف
                </button>
                <button class="cls-cancel-btn" data-bs-dismiss="modal">إلغاء</button>
            </div>
        </div>
    </div>
</div>

<!-- Update Modal -->
<div class="modal fade" id="clsEditModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content cls-modal">
            <div class="cls-modal-header">
                <button type="button" class="btn-close-x" data-bs-dismiss="modal"><i class="bi bi-x-lg"></i></button>
                <h5 class="cls-modal-title"><i class="bi bi-pencil-square" style="margin-left:8px;"></i>تحديث التصنيف</h5>
                <div class="cls-modal-subtitle">تعديل بيانات التصنيف</div>
            </div>
            <div class="cls-modal-body">
                <input type="hidden" id="clsEditId">
                <div class="cls-section-title"><i class="bi bi-info-circle-fill"></i>معلومات التصنيف</div>

                <div class="mb-3">
                    <label class="form-label">اسم التصنيف<span class="required-star">*</span></label>
                    <input type="text" class="form-control" id="clsEditName">
                </div>
                <div class="mb-3">
                    <label class="form-label">الوصف</label>
                    <textarea class="form-control" id="clsEditDescription" rows="3"></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label">الترتيب</label>
                    <input type="number" class="form-control" id="clsEditSortOrder" min="1">
                </div>
                      <div class="col-6">
                        <label class="form-label">اللون</label>
                        <div class="d-flex align-items-center gap-3">
                            <div class="cls-color-picker-wrap">
                                <input type="color" id="clsEditColor" value="#25935F" oninput="document.getElementById('clsEditColorHex').textContent=this.value">
                            </div>
                            <div class="cls-color-hex" id="clsEditColorHex" style="margin-top:0;">#25935F</div>
                        </div>
                    </div>
                </div>
                <div class="row mb-3">
                    <div class="col-6">
                        <label class="form-label">التفعيل</label>
                        <div class="d-flex align-items-center gap-3 mt-2">
                            <div class="form-check form-switch m-0">
                                <input class="form-check-input" type="checkbox" id="clsEditIsActive" style="width:48px;height:24px;">
                            </div>
                            <label class="form-check-label" for="clsEditIsActive" id="clsEditIsActiveLabel" style="font-weight:600;">مفعل</label>
                        </div>
                    </div>
              

                <div id="clsEditError" class="alert alert-danger d-none" style="font-size:14px;"></div>
            </div>
            <div class="cls-modal-footer">
                <button class="cls-save-btn" onclick="clsSubmitEdit()">
                    <i class="bi bi-check-lg"></i>حفظ التعديلات
                </button>
                <button class="cls-cancel-btn" data-bs-dismiss="modal">إلغاء</button>
            </div>
        </div>
    </div>
</div>

<!-- Details Modal -->
<div class="modal fade" id="clsDetailsModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content cls-modal">
            <div class="cls-modal-header" style="background:var(--info-700);">
                <button type="button" class="btn-close-x" data-bs-dismiss="modal"><i class="bi bi-x-lg"></i></button>
                <h5 class="cls-modal-title"><i class="bi bi-eye" style="margin-left:8px;"></i>تفاصيل التصنيف</h5>
                <div class="cls-modal-subtitle">عرض بيانات التصنيف</div>
            </div>
            <div class="cls-modal-body" id="clsDetailsBody">
            </div>
            <div class="cls-modal-footer" style="justify-content:flex-end;">
                <button class="cls-cancel-btn" data-bs-dismiss="modal">إغلاق</button>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirm Modal -->
<div class="modal fade" id="clsDeleteModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered modal-sm">
        <div class="modal-content cls-modal">
            <div class="cls-modal-header" style="background:var(--error-600);">
                <button type="button" class="btn-close-x" data-bs-dismiss="modal"><i class="bi bi-x-lg"></i></button>
                <h5 class="cls-modal-title"><i class="bi bi-trash3" style="margin-left:8px;"></i>حذف التصنيف</h5>
            </div>
            <div class="cls-modal-body text-center">
                <input type="hidden" id="clsDeleteId">
                <p style="font-weight:700;font-size:16px;margin-top:8px;color:var(--gray-800);">هل أنت متأكد من حذف هذا التصنيف؟</p>
                <p style="font-size:14px;color:var(--gray-500);" id="clsDeleteNameLabel"></p>
                <div id="clsDeleteError" class="alert alert-danger d-none" style="font-size:14px;text-align:right;"></div>
            </div>
            <div class="cls-modal-footer" style="justify-content:center;">
                <button class="cls-save-btn" style="background:var(--error-600);" onclick="clsSubmitDelete()">
                    نعم، حذف
                </button>
                <button class="cls-cancel-btn" data-bs-dismiss="modal">إلغاء</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
<script src="~/js/classifications.js?v=@DateTime.Now.Ticks"></script>
}
